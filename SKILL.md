---
name: music-studio
description: 弦外作曲规范：乐谱 JSON 结构、32 种音色、拍号与律动规则，用于把音乐交给 play_score 卡片。
---

# 弦外 · 作曲规范

用户想听音乐或音效时，你写出一张完整的乐谱 JSON，用 `play_score({ score: {...} })` 交给对话里的卡片。卡片负责画卷帘图、播放、导出；**作曲是你的活**。

写谱前先决定体裁：**旋律**（有节拍与音高）还是**音效**（氛围与形态）。两者的 JSON 结构不同。

## 一、旋律（`mode: "melody"`）

```json
{
  "title": "雨夜",
  "mood": "湿冷，远处有钟声",
  "mode": "melody",
  "meter": "4/4",
  "bpm": 96,
  "bars": 8,
  "tracks": [
    { "name": "低音", "wave": "bass", "gain": 0.22,
      "notes": [["C2", 0, 2, 0.7], ["G2", 2, 2, 0.55]] },
    { "name": "和声", "wave": "epiano", "gain": 0.14,
      "notes": [["Eb4", 0, 4, 0.5], ["Bb3", 4, 4, 0.45]] },
    { "name": "旋律", "wave": "choir", "gain": 0.16,
      "notes": [["G4", 0, 1.5, 0.85], ["Ab4", 1.5, 0.5, 0.7], ["Bb4", 2, 2, 0.9]] }
  ],
  "percussion": [["kick", 0, 0.8], ["hat", 2, 0.35], ["snare", 4, 0.5]],
  "groove": "straight",
  "fx": { "reverb": 0.4 }
}
```

### 字段口径

| 字段 | 含义与范围 |
|---|---|
| `title` | 中文曲名，超长会被截到 28 字 |
| `mood` | 中文意象描述（卡片上的副标题），截到 48 字 |
| `meter` | 拍号字符串，取值见下文「拍号」，认不出就按 `4/4` |
| `bpm` | 整数，钳到 **40–200** |
| `bars` | 小节数，从 1 起；上限**随拍号变化**（见「拍号」表） |
| `tracks[]` | 音轨数组，最多 **8** 条（建议 3–4 条） |
| `tracks[].name` | 音轨名，截到 16 字 |
| `tracks[].wave` | 音色名，必须出自下面的「音色清单」；不认识的会被换成 `triangle` 并给提示 |
| `tracks[].gain` | 该轨音量，钳到 **0.01–0.6**（默认 0.18） |
| `tracks[].notes[]` | 每条 `[音高, 起始拍, 时值拍, 力度]` |
| `notes[].音高` | 音名如 `"C4"` `"F#3"` `"Bb5"`，**建议落在 C2–C7**；硬钳位是 MIDI 24–100 |
| `notes[].起始拍` | 相对本曲第一拍的偏移，钳到 **0 – bars × 每小节拍数** |
| `notes[].时值拍` | 钳到 **0.05–16** 拍 |
| `notes[].力度` | 钳到 **0.05–1** |
| `percussion[]` | 每条 `[鼓件, 拍位, 力度]`，最多 **1500** 条 |
| `percussion[].鼓件` | 只能是 `kick` / `snare` / `hat` / `clap` / `tom`；**其他值会被直接丢掉** |
| `fx.reverb` | 混响量，钳到 **0–0.9**（旋律默认 0.34，音效默认 0.46） |
| `groove` | 律动档位，取值见「六、律动」；**不写就是直拍**；名字写错会被明说并按直拍处理。只对旋律生效 |

### 可选：曲式段落 `sections[]`

给上它，卷帘图上会画出结构条，整曲的骨架一眼可见：

```json
"sections": [
  { "label": "起", "role": "主题引入", "fromBar": 1, "toBar": 4 },
  { "label": "承", "role": "加入旋律", "fromBar": 5, "toBar": 8 }
]
```

最多 **24** 段；`label` 截到 6 字、`role` 截到 14 字；`fromBar` 从 1 起，`toBar` 不小于 `fromBar`。

## 二、音效（`mode: "sfx"`）

```json
{
  "title": "铁轨",
  "mood": "远处钟声，湿光",
  "mode": "sfx",
  "bpm": 72,
  "layers": [
    { "src": "noise", "type": "bandpass", "f0": 1200, "f1": 300, "q": 1.6,
      "start": 0, "dur": 3.2, "attack": 0.4, "decay": 1.8, "gain": 0.35, "pan": -0.3 },
    { "src": "tone", "wave": "bell", "type": "none", "f0": 440, "f1": 440, "q": 0.7,
      "start": 1.6, "dur": 2.4, "attack": 0.002, "decay": 0.9, "gain": 0.28, "pan": 0.35 }
  ]
}
```

| 字段 | 含义与范围 |
|---|---|
| `layers[]` | 最多 **16** 层（建议 4–8 层） |
| `src` | `"noise"` 或 `"tone"` |
| `wave` | 仅 `src: "tone"` 有意义；取值同音色清单 |
| `type` | 噪声层的滤波器：`lowpass` / `highpass` / `bandpass` / `none` |
| `f0` | 起始频率（Hz），钳到 **20–18000** |
| `f1` | 结束频率（扫描终点）：`tone` 扫振荡器音高，`noise` 扫滤波器截止；`f1 = f0` 表示不扫 |
| `q` | 滤波 Q，钳到 **0.1–24**（噪声层默认 1.2，音调层默认 0.7） |
| `start` | 起始秒，钳到 **0–60** |
| `dur` | 时长秒，钳到 **0.05–30** |
| `attack` | 起音秒，钳到 **0.001–8** |
| `decay` | 衰减秒，钳到 **0.01–12** |
| `gain` | 钳到 **0.01–1** |
| `pan` | 立体声位置，钳到 **-1–1** |

曲长由 `start + dur + decay` 的最大值决定，不会被 `bpm` 影响太多。

## 三、硬约束（会被自动钳位，但最好一开始就写对）

- 全曲音符总数上限 **6000**，超出直接截断（并给提示）。
- 打击乐上限 **1500** 条。
- 音效层上限 **16** 层。
- 建议每小节的音符数不超过 **14** 个、打击乐不超过 **5** 个——超过这个密度，卷帘图上会糊成一团，听感也糊。
- 所有越界值都会被**钳位而不是报错**，但工具小结会明说钳了什么。看到小结里有钳位提示，说明你原谱写得不合法。

## 四、音色清单

- `sine` —— 正弦（只有基频，最纯净，适合低音垫底）
- `triangle` —— 三角（柔和，泛音少）
- `sawtooth` —— 锯齿（明亮厚实，适合主奏与贝斯）
- `square` —— 方波（空心电子味，适合复古音色）
- `organ` —— 管风琴（庄严饱满，适合铺底与圣咏感）
- `brass` —— 铜管（明亮有冲劲，适合高潮段的主奏）
- `strings` —— 弦乐（连绵温暖，适合长音铺底）
- `pluck` —— 拨弦（颗粒分明，适合分解和弦）
- `marimba` —— 马林巴（圆润木质感，适合轻快中音区）
- `pad` —— 铺垫（宽广柔和，适合氛围背景）
- `reed` —— 簧管（略带鼻音，适合抒情旋律）
- `bass` —— 低音（厚实有轮廓，低音线的首选）
- `bell` —— 钟琴（清亮悠长，适合点缀与钟声意象）
- `piano` —— 钢琴（自然的敲击起振，通用性最强）
- `metal` —— 金属（敲击感的金属嗡鸣，适合冷硬意象）
- `lead` —— 合成主奏（穿透力强，适合电子主旋律）
- `choir` —— 人声（低音区的元音共振，像人声吟唱）
- `flute` —— 长笛（清亮带气声，适合空灵旋律）
- `epiano` —— 电钢（FM 敲击、余音清亮，适合和声层）
- `harpsichord` —— 羽管键琴（拨键亮起振、暗余音，适合巴洛克感）
- `hammond` —— 爵士风琴（音轮风琴，低音叠 16′，适合蓝调与爵士）
- `clarinet` —— 单簧管（奇次谐波簧管，中低音区的木管）
- `oboe` —— 双簧管（鼻音明显，中高音区的穿透木管）
- `horn` —— 圆号（软铜管、起振慢，适合辽阔的长音）
- `harp` —— 竖琴（软拨弦、余音长，适合流动的琶音）
- `xylophone` —— 木琴（硬木敲击、高音明亮，适合轻快点缀）
- `musicbox` —— 八音盒（FM 梳齿带机械噪声，适合怀旧与童趣）
- `kalimba` —— 卡林巴（拇指琴，只留基频，干净通透）
- `accordion` —— 手风琴（双簧拍频，适合民谣与欧洲味）
- `shakuhachi` —— 尺八（气声竹笛、起振慢，适合苍凉意象）
- `sitar` —— 西塔琴（拨弦加桥丘共振，适合东方色彩）
- `acidbass` —— 酸性贝斯（锯齿加低通下扫，适合电子舞曲）

### 音区自适应不用你操心

引擎会自动做三档处理：**低音厚、中音原样、高音瘦**（典型做法是低音区加强共鸣、高音区让滤波器少跟键、越高越暗或越纯）。这件事**零额外开销**，也不需要你在谱子里补偿。上面每条音色的说明里写了它在各音区的行为，选音色时按它来挑就行。

## 五、拍号

| 拍号 | 每小节拍数 | 性格与用法 | 小节数上限 |
|---|---|---|---|
| `4/4` | 4 | 最通用，流行、摇滚、民谣 | 240 |
| `3/4` | 3 | 圆舞曲的摇晃感 | 320 |
| `2/4` | 2 | 进行曲的方正步伐 | 480 |
| `5/4` | 5 | 爵士与实验，一小节里多出一步 | 192 |
| `7/8` | 7 | 前卫摇滚，七个八分音的组合 | 138 |
| `6/8` | 2 | **复合拍**：两大拍，每拍三连分；摇篮曲、船歌 | 480 |
| `9/8` | 3 | **复合拍**：三大拍 | 320 |
| `12/8` | 4 | **复合拍**：四大拍；蓝调与灵魂乐 | 240 |

复合拍（6/8、9/8、12/8）的一「拍」是**附点四分音符**，所以 `bpm` 数的是大拍、每拍能三连分。这也是为什么摇摆律动在复合拍下不生效——它本身已经是三连音了。

## 六、律动

写**乐谱顶层的 `groove` 字段**（和 `meter`、`bpm` 同级），整曲套一档。只对 `mode: "melody"` 生效；音效没有节拍网格，这一栏会被忽略。

| 档位 | 怎么做 | 适用 |
|---|---|---|
| `straight` | 不处理 | 绝大多数情况 |
| `swing8_2` | 把半拍上的八分音符确定性地往后挪（2:1，三连音感） | 爵士、蓝调 |
| `swing8_3` | 同上但更硬（3:1，附点摇摆） | 摇摆爵士、大乐队 |
| `swing16_12` | 十六分音符摇摆（1.2:1） | breakbeat、UK garage |
| `swing16_15` | 十六分音符摇摆（1.5:1） | 更强的十六分律动 |
| `halftime` | 军鼓落第 3 拍、重音减半 | 半速感，沉重氛围 |
| `genre` | 不做处理，只要求你自己写标志性的鼓型 | 你自定曲风的时候 |

写摇摆时**按直拍写**细分，引擎会负责那一下挪动；已经摇摆过的音不会被二次挪动。复合拍下摇摆不生效。

## 七、怎么才像音乐

- **先定调**，然后整曲保持。别在四个小节里跑遍十二个音。
- **三个声部**：低音线（走根音与五度）、和声层（和弦或琶音）、旋律（一条能哼出来的线）。
- **留白**：旋律不要每个拍点都占满。长音后面接短音，句子之间要有呼吸。
- **动态**：用 `velocity` 做出起伏（主旋律 0.7–0.9、和声 0.4–0.6、低音 0.6–0.8），而不是所有音一个力度。
- **结构**：给 `sections[]`，让曲子有起承转合；别把同一个两小节循环复制四遍。
- **音区分工**：低音线别写到中音区、旋律别沉到低音区，否则会互相打架。

## 八、修订

用户提意见时（「低音太密」「别那么亮」「从第 3 小节起改成每两拍一个长音」）：

1. **重写整张谱**，再交一次 `play_score`。不留旧版，卡片只显示最新一版。
2. 保留用户没提的部分，只改他说的。
3. 如果工具返回的小结里有「自动钳位」提示，说明上一次哪里不合法，这一版改掉。

## 九、调用示例

```
play_score({
  score: {
    title: "雾江",
    mood: "暮色四合，江水微凉，一叶孤舟",
    mode: "melody",
    meter: "4/4",
    bpm: 72,
    bars: 8,
    tracks: [
      { name: "低音", wave: "bass", gain: 0.24,
        notes: [["C2", 0, 4, 0.65], ["A1", 4, 4, 0.6], ["F1", 8, 4, 0.6], ["G1", 12, 4, 0.6]] },
      { name: "和声", wave: "pad", gain: 0.12,
        notes: [["Eb4", 0, 8, 0.45], ["C4", 8, 8, 0.42]] },
      { name: "旋律", wave: "shakuhachi", gain: 0.18,
        notes: [["G4", 0, 3, 0.8], ["Eb4", 3, 1, 0.6], ["F4", 4, 4, 0.85], ["G4", 10, 6, 0.9]] }
    ],
    percussion: [["kick", 0, 0.6], ["kick", 8, 0.55], ["hat", 4, 0.25], ["hat", 12, 0.25]],
    sections: [
      { label: "起", role: "雾中孤舟", fromBar: 1, toBar: 4 },
      { label: "展", role: "江面开阔", fromBar: 5, toBar: 8 }
    ],
    fx: { reverb: 0.5 }
  }
})
```

参数是 **JSON 对象**，不是字符串——传字符串会被参数校验直接拒掉。
